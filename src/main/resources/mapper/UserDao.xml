<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alisn.rs.dao.UserDao">

    <insert id="insert">
        INSERT IGNORE INTO USER_INFO (USER_ID, USER_NAME, USER_PASSWD)
        VALUES (UUID(), #{userName,jdbcType=VARCHAR}, #{userPasswd,jdbcType=VARCHAR})
    </insert>

    <update id="updatePasswd">
        UPDATE USER_INFO
        SET USER_PASSWD = #{userPasswd,jdbcType=VARCHAR}
        WHERE USER_NAME = #{userName,jdbcType=VARCHAR}
    </update>

    <select id="find" resultType="java.lang.Integer">
        SELECT count(*)
        FROM USER_INFO
        WHERE USER_NAME = #{userName,jdbcType=VARCHAR}
              AND USER_PASSWD = #{userPasswd,jdbcType=VARCHAR}
    </select>

    <select id="getByName" resultType="User">
        SELECT
            USER_ID,
            USER_NAME,
            USER_PASSWD,
            CREATE_TIME
        FROM USER_INFO
        WHERE USER_NAME = #{userName,jdbcType=VARCHAR}
    </select>
</mapper>